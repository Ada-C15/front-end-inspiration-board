import React from 'react';

const NewCardForm = (props) => {
    return (
        <form>
            {/* Message Input
            Submit button */}
        </form>
    );

};

export default NewCardForm;