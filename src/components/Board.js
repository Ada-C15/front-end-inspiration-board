import './Board.css';
import PropTypes from 'prop-types';

const Board = (props) => {
    return (
        <section>
            {/* props we need: board & onBoardSelect 
            onClick too*/}
        </section>
    );
}

export default Board;